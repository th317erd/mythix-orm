/* eslint-disable no-magic-numbers */

'use strict';

/* global describe, it, expect */

const { Utils } = require('../../lib');

describe('Utils::JSON', () => {
  describe('serialize', () => {
    it('should be able to serialize values to JSON', () => {
    });
  });

  describe('deserialize', () => {
    it('should be able to deserialize values from JSON', () => {
    });
  });
});
